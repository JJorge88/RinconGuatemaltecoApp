<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página de Inicio</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-5bGzyTlTthI6EInFTm8ItG6Evlj3YumF3Th5yGZbxemZo+lwGJ29BqlTpSMtfCUa" crossorigin="anonymous">
    <!-- Fuente de Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <!-- Estilos Personalizados -->
    <style>
        body {
            background-color: #E3F2FD; /* Fondo celeste claro */
            font-family: 'Poppins', sans-serif; /* Fuente amigable */
        }

        .inicio-container {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            text-align: center;
        }

        .inicio-card {
            background-color: #FFFFFF; /* Fondo blanco */
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
            max-width: 600px;
            width: 100%;
        }

        h1 {
            color: #1E88E5; /* Título celeste */
            font-weight: 600;
            margin-bottom: 20px;
        }

        p {
            color: #616161; /* Texto gris oscuro */
            font-size: 18px;
            margin-bottom: 30px;
        }

        .btn-primary {
            background-color: #42A5F5; /* Botón celeste */
            border: none;
            border-radius: 10px;
            font-size: 18px;
            font-weight: 500;
            padding: 10px 20px;
        }

            .btn-primary:hover {
                background-color: #1E88E5; /* Botón celeste oscuro */
            }

        .btn-container {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            gap: 15px;
        }

            .btn-container a {
                text-decoration: none;
            }
    </style>
</head>
<body>
    <div class="inicio-container">
        <div class="inicio-card">
            <h1>Bienvenido al Sistema</h1>
            <p>Explora las diferentes opciones para administrar clientes, proveedores, productos y más.</p>
            <div class="btn-container">
                <a href="@Url.Action("Index", "Clientes")" class="btn btn-primary">Clientes</a>
                <a href="@Url.Action("Index", "Proveedores")" class="btn btn-primary">Proveedores</a>
                <a href="@Url.Action("Index", "Productos")" class="btn btn-primary">Productos</a>
                <a href="@Url.Action("Index", "Ventas")" class="btn btn-primary">Ventas</a>
                <!-- Botón para ingresar despachos -->
                <a href="@Url.Action("Create", "Despachos")" class="btn btn-primary">Ingresar Despacho</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (Opcional si necesitas interacción avanzada) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+HAMedN5Y1Y1w6eG+AMvr03Pb1Q==" crossorigin="anonymous"></script>
</body>
</html>
